<Window
    x:Class="RdpLauncher.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RdpLauncher"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Grid x:Name="AppTitleBar" Grid.Row="0" Background="Transparent">
            <TextBlock Text="RDP Launcher"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       VerticalAlignment="Center"
                       Margin="16,0,0,0"/>
        </Grid>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24" Spacing="16" MaxWidth="600">
                
                <TextBlock Text="RDP Connection Settings"
                           Style="{StaticResource TitleTextBlockStyle}"
                           Margin="0,8,0,16"/>

                <!-- Server Address -->
                <StackPanel Spacing="8">
                    <TextBlock Text="Server Address" 
                               Style="{StaticResource BodyStrongTextBlockStyle}"/>
                    <TextBox x:Name="ServerAddressTextBox"
                             PlaceholderText="server.domain.com"
                             TextChanged="ServerAddressTextBox_TextChanged"/>
                </StackPanel>

                <!-- Resolution Settings -->
                <TextBlock Text="Resolution"
                           Style="{StaticResource SubtitleTextBlockStyle}"
                           Margin="0,16,0,8"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Spacing="8">
                        <TextBlock Text="Width" 
                                   Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <NumberBox x:Name="WidthNumberBox"
                                   Value="1920"
                                   Minimum="800"
                                   Maximum="7680"
                                   SpinButtonPlacementMode="Inline"
                                   ValueChanged="ResolutionNumberBox_ValueChanged"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Spacing="8">
                        <TextBlock Text="Height" 
                                   Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <NumberBox x:Name="HeightNumberBox"
                                   Value="1080"
                                   Minimum="600"
                                   Maximum="4320"
                                   SpinButtonPlacementMode="Inline"
                                   ValueChanged="ResolutionNumberBox_ValueChanged"/>
                    </StackPanel>
                </Grid>

                <!-- Common Resolutions -->
                <StackPanel Spacing="8">
                    <TextBlock Text="Common Resolutions" 
                               Style="{StaticResource BodyStrongTextBlockStyle}"/>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Content="1920×1080" Click="SetResolution_1920x1080"/>
                        <Button Content="2560×1440" Click="SetResolution_2560x1440"/>
                        <Button Content="3840×2160" Click="SetResolution_3840x2160"/>
                    </StackPanel>
                </StackPanel>

                <!-- Monitor Position (Hard-coded options) -->
                <TextBlock Text="Monitor Position"
                           Style="{StaticResource SubtitleTextBlockStyle}"
                           Margin="0,16,0,8"/>

                <StackPanel Spacing="8">
                    <TextBlock Text="Select target monitor" 
                               Style="{StaticResource BodyStrongTextBlockStyle}"/>
                    <ComboBox x:Name="MonitorComboBox"
                              HorizontalAlignment="Stretch"
                              SelectionChanged="MonitorComboBox_SelectionChanged">
                        <ComboBoxItem Content="Primary Monitor (0, 0)" Tag="0,0" IsSelected="True"/>
                        <ComboBoxItem Content="Secondary Monitor Left (-1920, 0)" Tag="-1920,0"/>
                        <ComboBoxItem Content="Secondary Monitor Right (1920, 0)" Tag="1920,0"/>
                        <ComboBoxItem Content="Secondary Monitor Above (0, -1080)" Tag="0,-1080"/>
                        <ComboBoxItem Content="Secondary Monitor Below (0, 1080)" Tag="0,1080"/>
                    </ComboBox>
                </StackPanel>

                <!-- Launch Button -->
                <Button x:Name="LaunchButton"
                        Content="Launch RDP Connection"
                        Style="{StaticResource AccentButtonStyle}"
                        HorizontalAlignment="Stretch"
                        Padding="16,12"
                        Margin="0,24,0,0"
                        Click="LaunchButton_Click"
                        IsEnabled="False"/>

                <!-- Status Message -->
                <InfoBar x:Name="StatusInfoBar"
                         IsOpen="False"
                         Margin="0,8,0,0"/>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
